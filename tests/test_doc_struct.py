text_with_markers = '''Распределенная система управления оборудованием (изделие 5ПРМ, далее по тексту - РСУО) предназначена для подготовки и применения авиационного оборудования, а также для управления электро-гидроагрегатами створок грузовых отсеков и створкой ННПУ.
РСУО обеспечивает:
–	подготовку авиационного оборудования к применению;
–	управление тактическим и аварийным пуском (сбросом) АСП с АПУ, АКУ, БД, УВКУ, ВПУ;
–	сброс ПТБ;
–	управление с помощью ННПУ;
–	управление электро-гидроагрегатами створок грузовых отсеков, ВПУ и створкой ННПУ, ДПП и ТПП;
–	безопасность применения и эксплуатации на земле авиационного оборудования;
–	встроенный контроль (ВСК);
–	возможность регистрации основных параметров РСУО;
–	РСУО обеспечивает подготовку и применение изд. 170-1, 180, 715, 750, 760, 80, 810, К08Б, К047, К029Б, 65МЛ, Д9М2А, 07, С-06, Д7УШК, АБ, С-8, С-13.'''

# Разделим текст на строки для анализа
lines = text_with_markers.split('\n')

print('Анализ текста с маркерами:')
print('=' * 50)

unnumbered_markers = [
    '•', '◦', '▪', '▫', '⁃', '·', '·', '•', '-', '–', '—', '*', '†', '‡', '§'
]

for i, line in enumerate(lines):
    line = line.strip()
    if line:
        print(f'Строка {i+1}: \"{line}\"')
        
        # Проверяем маркеры
        found_marker = None
        for marker in unnumbered_markers:
            if line.startswith(marker):
                found_marker = marker
                break
        
        if found_marker:
            print(f'  -> НАЙДЕН МАРКЕР: \"{found_marker}\" (Unicode: U+{ord(found_marker):04X})')
            print(f'  -> Тип списка: unnumbered_list')
            # Очистка текста
            clean_text = line[len(found_marker):].strip()
            print(f'  -> Очищенный текст: \"{clean_text}\"')
        else:
            print(f'  -> МАРКЕР НЕ НАЙДЕН')
        
        print()

print('Сводка по маркерам:')
print(f'Всего строк: {len([l for l in lines if l.strip()])}')
marked_lines = sum(1 for line in lines if any(line.strip().startswith(m) for m in unnumbered_markers))
print(f'Строк с маркерами: {marked_lines}')